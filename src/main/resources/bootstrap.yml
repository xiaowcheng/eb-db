server:
  port: ${port:8084} # 项目监听端口，可根据项目实际情况进行修改
  servlet:
    context-path: /txcy
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7001/eureka/
     #defaultZone: http://10.1.60.110:8761/eureka/
spring:
  application:
    name: microservice-yellowpagelibbak-server
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
      uri: http://10.1.60.109:8763
     #   service-id: txcy-config-server
  rabbitmq:
    host: 10.1.60.110
    port: 5672
    username: test
    password: test
